[project]
name = "sales-organizer"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.0",
    "openpyxl>=3.1",
    "xlsxwriter>=3.1",
    "streamlit>=1.30",
    "Flask>=3.0",
]

[tool.vercel]
entrypoint = "api.app:app"
